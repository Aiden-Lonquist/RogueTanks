using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PowerUp : MonoBehaviour
{
    public string type;
    public Sprite powerUpImage;
    public float speed; // tread power up
    public float health; // body power up
    public float damage; // gun power up
    public float fireRate; // gun power up
    public float bulletSpeed; // gun power up
    public List<string> modifiers; // all power ups
    public int index;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
